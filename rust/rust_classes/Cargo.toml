[package]
name = "rust_classes"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
godot = { git = "https://github.com/godot-rust/gdext", branch = "master" }
godot_traits = { path = "../godot_traits" }
statig = "0.3.0"
